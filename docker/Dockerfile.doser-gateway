# Используем JRE 21 для запуска Scala приложения
FROM eclipse-temurin:21-jre AS runtime # Базовый образ JVM

# Рабочая директория
WORKDIR /opt/app # Каталог приложения

# Копируем собранный jar
COPY target/scala-3.3.6/doser-gateway.jar /opt/app/app.jar # Jar doser-gateway

# Открываем порт
EXPOSE 8086/tcp # Порт doser-gateway

# Команда запуска
ENTRYPOINT ["java", "-jar", "/opt/app/app.jar"] # Запуск приложения

